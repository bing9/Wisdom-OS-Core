{
  "name": "wisdom-os-core",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Portable intent, decision, context and feedback kernel for AI-managed work.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check cli.mjs && node --check src/domain.mjs && node --check src/app.mjs && npm test"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "keywords": ["decision-os", "ai-agents", "kanban", "context-engineering", "claude-code", "codex"]
}
